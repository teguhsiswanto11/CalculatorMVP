<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
    
    <style name="number" parent="TextAppearance.AppCompat">
        <item name="android:textSize">36sp</item>
        <item name="android:background">@color/numberBotton</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:minHeight">75dp</item>
    </style>
    <style name="number.textSmall" parent="number">
        <item name="android:textSize">20sp</item>
    </style>

    <style name="number.operator" parent="number">
        <item name="android:textColor">#16a085</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
